CREATE OR REPLACE TYPE SHARE_type
AS OBJECT
(
   SHARE_NAME VARCHAR(500),
   SHARE_SYMBOL VARCHAR(500),
   SHARE_BOOK_COST_DATE VARCHAR(500),
   SHARE_AVERAGE_COST VARCHAR(500),
   SHARE_BOOK_COST VARCHAR(500),
   SHARE_QUANTITY VARCHAR(500)
);